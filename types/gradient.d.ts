declare module '~/public/scripts/Gradient.js' {
    export class Gradient {
      initGradient(selector: string): Gradient;
      // Add other method declarations if they exist in your class
    }
  }